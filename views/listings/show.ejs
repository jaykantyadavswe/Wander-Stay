<% layout("/layouts/boilerplate") %>

<div class="listing-details-page">
    <!-- Enhanced Hero Section with Image Gallery -->
    <div class="listing-hero">
        <div class="gallery-container">
            <!-- Main Image -->
            <div class="gallery-main">
                <img id="mainImage" src="<%= listing.image.url %>" alt="<%= listing.title %>" class="main-image">
                <div class="gallery-overlay">
                    <button class="gallery-nav prev" onclick="changeImage(-1)" type="button">
                        <i class="fa-solid fa-chevron-left"></i>
                    </button>
                    <button class="gallery-nav next" onclick="changeImage(1)" type="button">
                        <i class="fa-solid fa-chevron-right"></i>
                    </button>
                    <button class="wishlist-icon" type="button" onclick="toggleWishlist()">
                        <i class="fa-regular fa-heart"></i>
                    </button>
                    <button class="share-icon" type="button" onclick="shareProperty()">
                        <i class="fa-solid fa-share-nodes"></i>
                    </button>
                </div>

                <!-- Image Counter -->
                <div class="image-counter">
                    <span id="imageNumber">1</span>/<span id="totalImages">4</span>
                </div>
            </div>

            <!-- Thumbnail Gallery -->
            <div class="gallery-thumbnails">
                <button class="thumbnail-item active" onclick="selectImage(0)" type="button">
                    <img src="<%= listing.image.url %>" alt="Gallery image 1" class="thumbnail-img">
                    <div class="thumbnail-overlay"></div>
                </button>
                <button class="thumbnail-item" onclick="selectImage(1)" type="button">
                    <img src="<%= listing.image.url %>" alt="Gallery image 2" class="thumbnail-img" style="object-position: center top;">
                    <div class="thumbnail-overlay"></div>
                </button>
                <button class="thumbnail-item" onclick="selectImage(2)" type="button">
                    <img src="<%= listing.image.url %>" alt="Gallery image 3" class="thumbnail-img" style="object-position: center bottom;">
                    <div class="thumbnail-overlay"></div>
                </button>
                <button class="thumbnail-item" onclick="selectImage(3)" type="button">
                    <img src="<%= listing.image.url %>" alt="Gallery image 4" class="thumbnail-img" style="object-position: right center;">
                    <div class="thumbnail-overlay"></div>
                    <div class="thumbnail-badge">+4</div>
                </button>
            </div>
        </div>
    </div>

    <div class="listing-container">
        <!-- Main Content -->
        <div class="listing-main">
            <!-- Title & Header -->
            <div class="listing-title-section">
                <div>
                    <h1 class="property-title"><%= listing.title %></h1>
                    <div class="listing-meta">
                        <span class="rating">
                            <i class="fa-solid fa-star"></i>
                            4.92 (128 reviews)
                        </span>
                        <span class="location-tag">
                            <i class="fa-solid fa-location-dot"></i>
                            <%= listing.location %>, <%= listing.country %>
                        </span>
                    </div>
                </div>
                <div class="action-buttons">
                    <a href="/listings/<%= listing._id %>/edit" class="btn-secondary">
                        <i class="fa-solid fa-pen"></i> Edit
                    </a>
                    <form action="/listings/<%= listing._id %>?_method=DELETE" method="post" class="delete-form">
                        <button type="submit" class="btn-danger" onclick="return confirm('Are you sure?')">
                            <i class="fa-solid fa-trash"></i> Delete
                        </button>
                    </form>
                </div>
            </div>

            <!-- Description -->
            <div class="section description-section">
                <h2 class="section-title">About this place</h2>
                <p class="description-text"><%= listing.description %></p>
            </div>

            <!-- Amenities -->
            <div class="section amenities-section">
                <h2 class="section-title">What this place offers</h2>
                <div class="amenities-grid">
                    <% if(listing.amenities && listing.amenities.length > 0) { %>
                        <% listing.amenities.forEach(amenity => { %>
                            <div class="amenity-item">
                                <% if(amenity === 'wifi') { %>
                                    <i class="fa-solid fa-wifi"></i>
                                    <div><h4>WiFi</h4><p>High-speed internet</p></div>
                                <% } else if(amenity === 'kitchen') { %>
                                    <i class="fa-solid fa-kitchen-set"></i>
                                    <div><h4>Kitchen</h4><p>Full equipped</p></div>
                                <% } else if(amenity === 'parking') { %>
                                    <i class="fa-solid fa-car"></i>
                                    <div><h4>Parking</h4><p>Free parking</p></div>
                                <% } else if(amenity === 'pool') { %>
                                    <i class="fa-solid fa-swimming-pool"></i>
                                    <div><h4>Pool</h4><p>Swimming pool</p></div>
                                <% } else if(amenity === 'gym') { %>
                                    <i class="fa-solid fa-dumbbell"></i>
                                    <div><h4>Gym</h4><p>Fitness center</p></div>
                                <% } else if(amenity === 'ac') { %>
                                    <i class="fa-solid fa-fan"></i>
                                    <div><h4>Air Conditioning</h4><p>Climate control</p></div>
                                <% } else if(amenity === 'tv') { %>
                                    <i class="fa-solid fa-tv"></i>
                                    <div><h4>Smart TV</h4><p>Streaming ready</p></div>
                                <% } else if(amenity === 'washer') { %>
                                    <i class="fa-solid fa-shirt"></i>
                                    <div><h4>Washer</h4><p>Laundry facilities</p></div>
                                <% } %>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <div class="amenity-item">
                            <i class="fa-solid fa-wifi"></i>
                            <div><h4>WiFi</h4><p>High-speed internet</p></div>
                        </div>
                        <div class="amenity-item">
                            <i class="fa-solid fa-kitchen-set"></i>
                            <div><h4>Kitchen</h4><p>Full equipped</p></div>
                        </div>
                        <div class="amenity-item">
                            <i class="fa-solid fa-car"></i>
                            <div><h4>Parking</h4><p>Free parking</p></div>
                        </div>
                        <div class="amenity-item">
                            <i class="fa-solid fa-fan"></i>
                            <div><h4>Air Conditioning</h4><p>Climate control</p></div>
                        </div>
                    <% } %>
                </div>
            </div>

            <!-- Room Details -->
            <div class="section room-details-section">
                <h2 class="section-title">Room Details</h2>
                <div class="details-grid">
                    <div class="detail-item">
                        <span class="detail-label">Bedrooms</span>
                        <span class="detail-value"><%= listing.bedrooms || 1 %></span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Bathrooms</span>
                        <span class="detail-value"><%= listing.bathrooms || 1 %></span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Guests</span>
                        <span class="detail-value"><%= listing.maxGuests || 2 %></span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Area</span>
                        <span class="detail-value"><%= listing.area || 500 %> <span class="sqft">sq.ft</span></span>
                    </div>
                </div>
            </div>

            <!-- Reviews Section -->
            <div class="section reviews-section">
                <h2 class="section-title">Guest Reviews</h2>
                <div class="reviews-container">
                    <div class="review-card">
                        <div class="review-header">
                            <div class="reviewer-info">
                                <div class="reviewer-avatar">A</div>
                                <div>
                                    <h4>Amit Kumar</h4>
                                    <p>Stayed 3 nights • 2 months ago</p>
                                </div>
                            </div>
                            <div class="review-rating">
                                <i class="fa-solid fa-star"></i>
                                <span>4.8</span>
                            </div>
                        </div>
                        <p class="review-text">Amazing place! The host was very welcoming and the property is exactly as shown in the pictures. Great location near the city center.</p>
                    </div>

                    <div class="review-card">
                        <div class="review-header">
                            <div class="reviewer-info">
                                <div class="reviewer-avatar">P</div>
                                <div>
                                    <h4>Priya Singh</h4>
                                    <p>Stayed 2 nights • 1 month ago</p>
                                </div>
                            </div>
                            <div class="review-rating">
                                <i class="fa-solid fa-star"></i>
                                <span>5.0</span>
                            </div>
                        </div>
                        <p class="review-text">Perfect for a weekend getaway! Everything was clean and well-maintained. Would definitely come back.</p>
                    </div>
                </div>
            </div>

            <!-- Location Map -->
            <div class="section location-section">
                <h2 class="section-title">Where you'll be</h2>
                <div class="map-placeholder">
                    <i class="fa-solid fa-map"></i>
                    <p>Map will be displayed here</p>
                    <small><%= listing.location %>, <%= listing.country %></small>
                </div>
            </div>
        </div>

        <!-- Sidebar - Booking Card -->
        <div class="listing-sidebar">
            <div class="booking-card">
                <div class="price-section">
                    <span class="price">&#8377; <%= listing.price.toLocaleString("en-IN") %></span>
                    <span class="period">/night</span>
                </div>

                <div class="booking-form">
                    <div class="form-group">
                        <label>Check In</label>
                        <input type="date" class="form-input" placeholder="Add date">
                    </div>
                    <div class="form-group">
                        <label>Check Out</label>
                        <input type="date" class="form-input" placeholder="Add date">
                    </div>
                    <div class="form-group">
                        <label>Guests</label>
                        <select class="form-input">
                            <option>1 Guest</option>
                            <option>2 Guests</option>
                            <option>3 Guests</option>
                            <option>4 Guests</option>
                        </select>
                    </div>
                </div>

                <button class="btn-book" type="button" onclick="bookNow()">Book Now</button>

                <div class="price-breakdown">
                    <div class="breakdown-item">
                        <span>&#8377; <%= listing.price.toLocaleString("en-IN") %> × 5 nights</span>
                        <span>&#8377; <%= (listing.price * 5).toLocaleString("en-IN") %></span>
                    </div>
                    <div class="breakdown-item">
                        <span>Service fee</span>
                        <span>&#8377; 2000</span>
                    </div>
                    <div class="breakdown-item total">
                        <span>Total before taxes</span>
                        <span>&#8377; <%= (listing.price * 5 + 2000).toLocaleString("en-IN") %></span>
                    </div>
                </div>

                <div class="host-info-card">
                    <h3>About the host</h3>
                    <div class="host-profile">
                        <div class="host-avatar">R</div>
                        <div>
                            <h4>Raj Patel</h4>
                            <p>Superhost</p>
                        </div>
                    </div>
                    <button class="btn-message" type="button">
                        <i class="fa-solid fa-message"></i> Message Host
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    let currentImageIndex = 0;
    // Gallery images - can be extended to pull from database if needed
    const images = [
        '<%= listing.image.url %>',
        '<%= listing.image.url %>',
        '<%= listing.image.url %>',
        '<%= listing.image.url %>'
    ];

    // Initialize total images count
    document.addEventListener('DOMContentLoaded', function() {
        document.getElementById('totalImages').textContent = images.length;
    });

    function changeImage(direction) {
        currentImageIndex = (currentImageIndex + direction + images.length) % images.length;
        updateGallery();
    }

    function selectImage(index) {
        currentImageIndex = index;
        updateGallery();
    }

    function updateGallery() {
        // Update main image
        const mainImage = document.getElementById('mainImage');
        mainImage.style.opacity = '0';
        
        setTimeout(() => {
            mainImage.src = images[currentImageIndex];
            mainImage.style.opacity = '1';
        }, 150);

        // Update image counter
        document.getElementById('imageNumber').textContent = currentImageIndex + 1;

        // Update thumbnail selection
        document.querySelectorAll('.thumbnail-item').forEach((thumb, index) => {
            thumb.classList.toggle('active', index === currentImageIndex);
        });
    }

    function toggleWishlist() {
        const btn = document.querySelector('.wishlist-icon');
        btn.classList.toggle('active');
        
        // Haptic feedback for mobile
        if (navigator.vibrate) {
            navigator.vibrate(50);
        }
    }

    function shareProperty() {
        if (navigator.share) {
            navigator.share({
                title: '<%= listing.title %>',
                text: 'Check out this amazing property!',
                url: window.location.href
            });
        } else {
            // Fallback for browsers that don't support share API
            alert('Share this property: ' + window.location.href);
        }
    }

    function bookNow() {
        alert('Booking feature coming soon!');
    }

    // Keyboard navigation for gallery
    document.addEventListener('keydown', function(event) {
        if (event.key === 'ArrowLeft') {
            changeImage(-1);
        } else if (event.key === 'ArrowRight') {
            changeImage(1);
        }
    });

    // Style transitions
    const mainImage = document.getElementById('mainImage');
    if (mainImage) {
        mainImage.style.transition = 'opacity 0.3s ease-in-out';
    }
</script>